# Programming Fundamentals - Assignment 3 (Part 3)

This section of the repository contains the final set of C++ programs for Assignment 3, focusing on custom string manipulation, 2D arrays, and mathematical algorithms.

## Author Details
* **Author:** Amaz Majeed
* **Roll Number:** 048
* **Class Section:** SE-2A
* **Institution:** Superior University (BSSE)

---

## Assignment Overview

Below is a breakdown of the included programs:

*   **String Copier:** A program containing a custom function that manually copies a specific number of characters from a source array into a destination array[cite: 28].
*   **Even and Odd Counter:** A script that takes a user-defined array of integers and explicitly counts the total number of both even and odd values[cite: 29].
*   **Seating Chart Matrix:** A program that generates a 2D array based on user-input rows and columns, allowing the user to populate the matrix to display a formatted seating chart[cite: 30].
*   **GCD Calculator:** A script that computes the Greatest Common Divisor (GCD) of two integers using a loop-based Euclidean algorithm[cite: 31].
*   **Strike Rate Calculator:** A program that evaluates a cricket batter's performance by calculating their strike rate based on the runs scored and balls faced[cite: 32].
*   **String Length Evaluator:** A script featuring a custom function that determines the exact length of a character array by counting elements until a null terminator is reached, bypassing standard library functions[cite: 33].

---

## Compilation and Execution

To compile and run any of these programs, use a standard C++ compiler like `g++` in your terminal.

**1. Compile the code:**
```bash
g++ filename.cpp -o outputname
